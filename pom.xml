<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
		 xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		 xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.7</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>

	<groupId>com.example</groupId>
	<artifactId>Lean-DevSecOps</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<name>Lean-DevSecOps</name>
	<description>Lean DevSecOps Spring Boot Project for CI/CD and SonarCloud Integration</description>

	<properties>
		<java.version>17</java.version>
	</properties>

	<dependencies>
		<!-- Spring Boot Web Starter -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<!-- Spring Boot Test Starter -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!-- A deliberately vulnerable dependency for SCA testing -->
		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-core</artifactId>
			<version>2.14.1</version>
		</dependency>

	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<!-- This is the new plugin configuration -->
			<plugin>
				<groupId>org.owasp</groupId>
				<artifactId>dependency-check-maven</artifactId>
				<version>9.2.0</version> <!-- Use a recent, specific version -->
				<configuration>
					<!-- This is the critical tag -->
					<!-- It tells the build to FAIL if any vulnerability -->
					<!-- with a CVSS score of 9.0 or higher is found. -->
					<!-- Log4Shell has a score of 10.0, so this will trigger. -->
					<failBuildOnCVSS>9.0</failBuildOnCVSS>

					<!-- NEW LINE ADDED TO FIX 403 ERROR -->
					<!-- Adds a 3-second delay between NVD API calls -->
					<nvdApiDelay>3000</nvdApiDelay>
					
				</configuration>
				<!-- 
                This executions block was removed in the previous step to stop it 
                from running automatically during 'verify'. 
                The main.yml file now calls 'check' manually.
                -->
			</plugin>
		</plugins>
	</build>

</project>